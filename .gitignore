# Build output
/quizme
/dist/
/bin/

# Drawn from icon.svg by `make icon`, and bundled by `make bundle`. Both are
# built from the drawing, so neither is kept.
/icon.png
/Quizme.app/

# Go
*.test
*.out

# macOS
.DS_Store

# Generated from skills/quizme/SKILL.md by `make skill`, so this
# repository's own agents see the skill without installing it.
/.claude/skills/quizme/
